import UIKit
import Foundation


//Write a Swift program to test whether the last digit of the two given non-negative integer values are same or not.


func checkLastDigit(firstInt: Int, secondInt:Int){
    let lastDigit = firstInt.words.endIndex
    print(lastDigit)
}

checkLastDigit(firstInt: 10, secondInt: 12)
